
// 自动生成：/make/item/make98a

inherit ITEM;
inherit COMBINED;

int get_max_combined() { return 10000; }

// 函数：构造处理
void create()
{
        set_name("Thần Nông Bảo Điển");
        set_picid_1(9819);
        set_picid_2(9819);
        set_unit("本");
        set_value(3500);
        set_amount(1);
}

// 函数：获取描述
string get_desc() 
{ 
        return "Thư tịch ghi chép Thiên hạ kỳ hoa bách thảo,\n Vân Mộng Trạch có thể cất giấu kỳ thư này."; 
}
